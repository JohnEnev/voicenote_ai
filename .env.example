# Firebase Configuration (Optional - for cloud sync)
# Get these from Firebase Console > Project Settings > General
# Note: API keys and config will be in google-services.json (Android)
# and GoogleService-Info.plist (iOS) after running 'flutterfire configure'

# Cloud AI Provider (Optional - for cloud summarization)
OPENAI_API_KEY=your-openai-key
